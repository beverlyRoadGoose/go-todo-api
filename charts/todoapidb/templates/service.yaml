apiVersion: v1
kind: Service
metadata:
  name: {{ include "todoapidb.fullname" . }}
  labels:
    {{- include "todoapidb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todoapidb.selectorLabels" . | nindent 4 }}
