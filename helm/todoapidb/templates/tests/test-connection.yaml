apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todoapidb.fullname" . }}-test-connection"
  labels:
    {{- include "todoapidb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todoapidb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
